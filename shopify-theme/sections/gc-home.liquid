{%- comment -%} GummyChums homepage — coming-soon vibe, pink-forward, scroll fall-in kept {%- endcomment -%}
{%- assign s = section.settings -%}
<style>
  .gc-home{margin:0 calc(var(--gutter) * -1);overflow-x:clip;color:var(--c-ink)}
  .gc-home *{box-sizing:border-box}
  .gc-wrap{max-width:var(--container);margin-inline:auto;padding-inline:var(--gutter)}

  /* ---------- ACT 1 — hero + scroll fall-in ---------- */
  .gh-stage{position:relative;height:200vh}
  /* transparent so the global fixed flavour background shows through — nothing to repaint */
  .gh-pin{position:sticky;top:0;height:100svh;overflow:hidden;display:grid;place-items:center;
    background:transparent;contain:paint;padding-bottom:4vh}

  .gh-gummy{position:absolute;left:50%;bottom:-14%;width:min(22vw,150px);z-index:1;
    will-change:transform,opacity;backface-visibility:hidden;transform-origin:center bottom;
    opacity:calc(var(--z,0) * 5);
    transform:translate3d(-50%,0,0) scale(calc(1 + var(--z,0) * var(--z,0) * 20))}
  /* ---- gummy characters ---- */
  .gcg{width:100%;height:auto;display:block}
  .gh-char{position:absolute;z-index:1;will-change:transform;pointer-events:none;
    translate:var(--px,0) var(--py,0);
    animation:ghDrift ease-in-out infinite alternate}
  .gh-c1{width:clamp(120px,15vw,215px);left:4%;top:14%;animation-duration:7s}
  .gh-c2{width:clamp(88px,11vw,158px);right:7%;top:9%;animation-duration:9s;animation-delay:-2s}
  .gh-c3{width:clamp(105px,13vw,190px);right:3%;bottom:14%;animation-duration:8s;animation-delay:-4s}
  .gh-c4{width:clamp(70px,8vw,118px);left:11%;bottom:12%;animation-duration:10s;animation-delay:-6s}
  @keyframes ghDrift{from{transform:translate3d(0,0,0) rotate(-6deg)}to{transform:translate3d(0,-24px,0) rotate(6deg)}}
  @media(max-width:900px){.gh-c2,.gh-c4{display:none}
    .gh-c1{width:110px;left:-2%;top:11%}.gh-c3{width:100px;right:-2%;bottom:17%}}

  /* ---- rotated sticker badges ---- */
  .gh-stick{position:absolute;z-index:3;font-weight:var(--fw-black);font-size:var(--fs-xs);letter-spacing:var(--ls-wide);
    background:var(--c-surface);color:var(--c-ink);padding:var(--sp-2) var(--sp-4);border-radius:var(--r-full);white-space:nowrap;
    box-shadow:0 4px 0 var(--c-ink);border:2px solid var(--c-ink)}
  .gh-s1{left:7%;top:38%;transform:rotate(-9deg);background:var(--c-mango-soft)}
  .gh-s2{right:8%;top:44%;transform:rotate(8deg);background:var(--c-orange-soft)}
  .gh-s3{left:14%;bottom:26%;transform:rotate(5deg);background:var(--c-cherry-soft)}
  @media(max-width:900px){.gh-s1{left:2%;top:30%}.gh-s2{right:2%;top:auto;bottom:30%}.gh-s3{display:none}}

  .gh-mid{position:relative;z-index:2;text-align:center;padding:0 var(--sp-5);
    opacity:calc(1 - var(--z,0) * 2.7);will-change:opacity}
  .gh-eyebrow{display:inline-flex;align-items:center;gap:var(--sp-2);background:var(--c-ink);color:var(--c-surface);
    font-weight:var(--fw-black);font-size:var(--fs-xs);letter-spacing:var(--ls-wide);text-transform:uppercase;
    padding:var(--sp-2) var(--sp-4);border-radius:var(--r-full);margin-bottom:var(--sp-4)}
  .gh-tag mark{background:linear-gradient(180deg,transparent 58%,var(--c-accent-soft) 58%);
    color:inherit;padding:0 .06em}
  .gh-cta2{color:var(--c-ink);font-weight:var(--fw-black);font-size:var(--fs-base);text-decoration:none;
    display:inline-flex;align-items:center;gap:var(--sp-2);padding:var(--sp-1) var(--sp-1);border-bottom:2.5px solid var(--c-ink)}
  .gh-cta2 span{transition:transform var(--dur-2) var(--ease);display:inline-block}
  .gh-cta2:hover span{transform:translateX(5px)}
  .gh-logo{display:block;width:min(470px,74vw);height:auto;margin:0 auto var(--sp-3);
    -webkit-user-drag:none;user-select:none;-webkit-touch-callout:none;pointer-events:none}
  .gh-tag{font-size:var(--fs-4xl);font-weight:var(--fw-black);letter-spacing:var(--ls-tight);line-height:var(--lh-tight);margin:0 auto;max-width:17ch}
  .gh-sub{font-size:var(--fs-lg);font-weight:var(--fw-body);color:var(--c-ink-2);margin:var(--sp-3) auto 0;max-width:34ch;line-height:var(--lh-body)}
  .gh-cta{margin-top:var(--sp-5);display:flex;align-items:center;justify-content:center;gap:var(--sp-4);flex-wrap:wrap}
  .gh-pills{display:flex;gap:var(--sp-2);justify-content:center;flex-wrap:wrap;margin-top:var(--sp-4)}
  .gh-pill{background:var(--c-surface);border-radius:var(--r-full);padding:var(--sp-2) var(--sp-4);font-size:var(--fs-sm);font-weight:var(--fw-black);
    letter-spacing:var(--ls-normal);box-shadow:var(--sh-2);outline:1px solid var(--c-line);outline-offset:-1px}
  .gh-scroll{margin-top:var(--sp-4);font-weight:var(--fw-black);letter-spacing:var(--ls-wide);font-size:var(--fs-xs);color:var(--c-ink-3);
    animation:ghBounce 1.7s ease-in-out infinite}
  @keyframes ghBounce{0%,100%{transform:translateY(0)}50%{transform:translateY(7px)}}
  .gh-reveal{position:absolute;inset:0;z-index:3;display:grid;place-items:center;text-align:center;padding:0 var(--sp-5);
    color:var(--c-surface);opacity:calc((var(--z,0) - .60) * 3.8);pointer-events:none}
  .gh-reveal h2{font-size:var(--fs-4xl);font-weight:var(--fw-black);letter-spacing:var(--ls-tight);line-height:var(--lh-tight);margin:0}
  .gh-reveal p{font-size:var(--fs-xl);font-weight:var(--fw-bold);margin:var(--sp-4) auto 0;max-width:30ch;opacity:.92}

  /* ---------- ACT 2 — flavours ---------- */
  .gh-sec{padding:clamp(var(--sp-7),6vw,var(--sp-8)) 0}
  .gh-head{text-align:center;margin-bottom:clamp(var(--sp-5),4vw,var(--sp-7))}
  .gh-head h2{font-size:var(--fs-3xl);font-weight:var(--fw-black);letter-spacing:var(--ls-tight);line-height:var(--lh-tight);margin:var(--sp-2) 0 0}
  .gh-head p{font-size:var(--fs-lg);font-weight:var(--fw-body);color:var(--c-ink-2);margin:var(--sp-3) auto 0;max-width:40ch}
  .gh-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:clamp(var(--sp-4),2vw,var(--sp-5))}
  @media(max-width:900px){.gh-grid{grid-template-columns:1fr;max-width:430px;margin:0 auto}}

  .gh-card{background:var(--c-surface);border-radius:var(--r-lg);padding:var(--sp-5);display:flex;flex-direction:column;gap:var(--sp-3);
    box-shadow:var(--sh-2);transition:transform var(--dur-2) var(--spring),box-shadow var(--dur-2) var(--ease)}
  .gh-card:hover{transform:translateY(-8px);box-shadow:var(--sh-3)}

  /* shorter, balanced media ratio — no more empty 1:1 void */
  .gh-shot{position:relative;display:block;border-radius:var(--r-md);aspect-ratio:5/4;overflow:hidden;background:var(--pc-soft)}
  .gh-shot img{width:100%;height:100%;object-fit:cover;display:block}

  /* cluster of 2-3 gummies at different sizes/rotations — reads as product, not an icon */
  .gh-cluster{position:absolute;inset:0}
  .gh-g{position:absolute;display:block;will-change:translate;animation:ghBob ease-in-out infinite alternate}
  .gh-g1{width:44%;left:9%;top:19%;transform:rotate(-9deg);animation-duration:6.5s}
  .gh-g2{width:29%;right:9%;top:9%;transform:rotate(13deg);animation-duration:7.8s;animation-delay:-2.4s}
  .gh-g3{width:26%;left:36%;bottom:8%;transform:rotate(-15deg);animation-duration:7.1s;animation-delay:-4.2s}
  @keyframes ghBob{0%{translate:0 0}100%{translate:0 -9px}}

  .gh-badge{position:absolute;z-index:1;top:var(--sp-3);left:var(--sp-3);background:var(--c-surface);color:var(--c-ink);
    font-size:var(--fs-xs);font-weight:var(--fw-black);letter-spacing:var(--ls-wide);text-transform:uppercase;
    padding:var(--sp-2) var(--sp-3);border-radius:var(--r-full);box-shadow:var(--sh-1)}
  .gh-name{font-size:var(--fs-xl);font-weight:var(--fw-black);letter-spacing:var(--ls-tight);color:var(--c-ink);text-decoration:none;margin:0}
  .gh-line{font-size:var(--fs-sm);font-weight:var(--fw-body);color:var(--c-ink-2);margin:0;line-height:var(--lh-snug)}
  .gh-foot{display:flex;align-items:center;justify-content:space-between;gap:var(--sp-2);margin-top:auto;padding-top:var(--sp-1)}
  .gh-price{font-size:var(--fs-xl);font-weight:var(--fw-black)}
  .gh-add{border:0;cursor:pointer;font-family:inherit;font-weight:var(--fw-black);font-size:var(--fs-sm);color:var(--c-surface);
    padding:var(--sp-3) var(--sp-5);border-radius:var(--r-full);transition:transform var(--dur-1) var(--spring),filter var(--dur-2) var(--ease)}
  .gh-add:hover{filter:brightness(1.08)} .gh-add:active{transform:scale(.93)}
  .gh-add--soldout{background:var(--c-ink-3);cursor:default}
  .gh-empty{grid-column:1/-1;text-align:center;color:var(--c-ink-2)}

  /* ---------- ACT 3 — story ---------- */
  .gh-story{background:var(--c-ink);color:var(--c-bg-warm);border-radius:var(--r-lg);
    padding:clamp(var(--sp-7),6vw,var(--sp-8)) clamp(var(--sp-5),4vw,var(--sp-8));
    position:relative;overflow:hidden;text-align:center}
  .gh-story::before{content:"";position:absolute;inset:0;pointer-events:none;
    background:radial-gradient(48vw 48vw at 10% 0%, var(--c-accent-soft), transparent 62%)}
  .gh-story-in{position:relative;z-index:1}
  .gh-story h2{font-size:var(--fs-3xl);font-weight:var(--fw-black);letter-spacing:var(--ls-tight);line-height:var(--lh-snug);margin:0 auto;max-width:22ch}
  .gh-story h2 em{font-style:normal;color:var(--c-accent)}
  .gh-story p{font-size:var(--fs-lg);font-weight:var(--fw-body);opacity:.82;
    margin:var(--sp-4) auto 0;max-width:46ch;line-height:var(--lh-body)}
  .gh-vals{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:var(--sp-3);margin-top:var(--sp-6)}
  @media(max-width:749px){.gh-vals{grid-template-columns:1fr}}
  .gh-val{background:rgba(255,255,255,.06);border-radius:var(--r-md);padding:var(--sp-5)}
  .gh-val b{display:block;font-size:var(--fs-base);color:var(--c-accent);margin-bottom:var(--sp-1)}
  .gh-val span{font-size:var(--fs-sm);font-weight:var(--fw-body);opacity:.8}

  /* ---------- ACT 4 — FAQ ---------- */
  .gh-faq{max-width:760px;margin:0 auto}
  .gh-q{background:var(--c-surface);border-radius:var(--r-md);margin-bottom:var(--sp-3);overflow:hidden;box-shadow:var(--sh-2)}
  .gh-q summary{cursor:pointer;list-style:none;padding:var(--sp-4) var(--sp-5);font-weight:var(--fw-black);font-size:var(--fs-base);
    display:flex;align-items:center;justify-content:space-between;gap:var(--sp-3)}
  .gh-q summary::-webkit-details-marker{display:none}
  .gh-q summary::after{content:"+";font-size:var(--fs-xl);color:var(--c-accent);transition:transform var(--dur-3) var(--ease);line-height:1}
  .gh-q[open] summary::after{transform:rotate(45deg)}
  .gh-q p{margin:0;padding:0 var(--sp-5) var(--sp-5);font-weight:var(--fw-body);font-size:var(--fs-base);color:var(--c-ink-2);line-height:var(--lh-body)}

  /* ---------- ACT 5 — closing CTA ---------- */
  .gh-end{text-align:center;padding:clamp(var(--sp-8),8vw,var(--sp-10)) var(--sp-5)}
  .gh-end h2{font-size:var(--fs-3xl);font-weight:var(--fw-black);letter-spacing:var(--ls-tight);line-height:var(--lh-tight);margin:0 auto;max-width:16ch}
  .gh-end h2 em{font-style:normal;color:var(--c-accent)}
  .gh-end p{font-size:var(--fs-lg);font-weight:var(--fw-body);color:var(--c-ink-2);margin:var(--sp-4) auto var(--sp-6);max-width:36ch}

  @media (prefers-reduced-motion:reduce){
    .gh-stage{height:auto}.gh-pin{position:relative;height:auto;padding:70px 0}
    .gh-gummy{display:none}.gh-mid{opacity:1!important}.gh-reveal{display:none}
    .gh-g{animation:none}
  }
</style>

<div class="gc-home">

  <!-- ACT 1 -->
  <header class="gh-stage" id="gh-stage">
    <div class="gh-pin" id="gh-pin">

      {%- comment -%} gummy characters — parallax to cursor {%- endcomment -%}
      <div class="gh-char gh-c1" data-depth="26">{% render 'gc-gummy', tone: '--c-cherry', id: 'h1' %}</div>
      <div class="gh-char gh-c2" data-depth="-34">{% render 'gc-gummy', tone: '--c-mango', id: 'h2' %}</div>
      <div class="gh-char gh-c3" data-depth="20">{% render 'gc-gummy', tone: '--c-orange', id: 'h3' %}</div>
      <div class="gh-char gh-c4" data-depth="-16">{% render 'gc-gummy', tone: '--c-cherry', id: 'h4' %}</div>

      {%- comment -%} the one that zooms on scroll {%- endcomment -%}
      <div class="gh-gummy">{% render 'gc-gummy', tone: '--c-cherry', id: 'hz' %}</div>

      <div class="gh-mid">
        <span class="gh-eyebrow">{{ s.eyebrow }}</span>
        <img class="gh-logo" src="{{ 'gc-wordmark-stack.png' | asset_url }}" alt="{{ shop.name }}" width="680" height="254" draggable="false" oncontextmenu="return false">
        <h1 class="gh-tag">{{ s.headline | replace: '[', '<mark>' | replace: ']', '</mark>' }}</h1>
        <p class="gh-sub">{{ s.sub }}</p>
        <div class="gh-cta">
          <a class="gc-btn" href="{{ routes.all_products_collection_url }}">{{ s.cta }}</a>
          <a class="gh-cta2" href="#gc-story">Why gummies? <span>→</span></a>
        </div>
        <div class="gh-scroll">SCROLL ↓</div>
      </div>

      {%- comment -%} rotated sticker badges {%- endcomment -%}
      <span class="gh-stick gh-s1">100% VEG</span>
      <span class="gh-stick gh-s2">NO ADDED SUGAR</span>
      <span class="gh-stick gh-s3">MADE IN INDIA</span>

      <div class="gh-reveal">
        <div>
          <h2>{{ s.reveal_title }}</h2>
          <p>{{ s.reveal_sub }}</p>
        </div>
      </div>
    </div>
  </header>

  <!-- ACT 2 — flavours -->
  <section class="gh-sec" id="shop">
    <div class="gc-wrap">
      <div class="gh-head gc-rise">
        <p class="gc-kick">{{ s.shop_kicker }}</p>
        <h2>{{ s.shop_title }}</h2>
        <p>{{ s.shop_sub }}</p>
      </div>
      <div class="gh-grid">
        {%- for product in collections.all.products limit: 3 -%}
          {%- assign t = product.title | downcase -%}
          {%- if t contains 'melatonin' or t contains 'sleep' -%}
            {%- assign tone = '--c-cherry' -%}{%- assign tone_soft = '--c-cherry-soft' -%}
            {%- assign role = 'Sleep' -%}{%- assign benefit = 'Wind down. Sleep tight.' -%}
          {%- elsif t contains 'eye' -%}
            {%- assign tone = '--c-orange' -%}{%- assign tone_soft = '--c-orange-soft' -%}
            {%- assign role = 'Eye care' -%}{%- assign benefit = 'For screen-tired eyes.' -%}
          {%- else -%}
            {%- assign tone = '--c-mango' -%}{%- assign tone_soft = '--c-mango-soft' -%}
            {%- assign role = 'Focus' -%}{%- assign benefit = 'Big brain energy, tiny gummy.' -%}
          {%- endif -%}
          {%- assign gi = forloop.index -%}
          {%- assign gid_a = 'a' | append: gi -%}
          {%- assign gid_b = 'b' | append: gi -%}
          {%- assign gid_c = 'c' | append: gi -%}
          <div class="gh-card gc-rise" style="transition-delay:{{ forloop.index0 | times: 90 }}ms">
            <a href="{{ product.url }}" class="gh-shot" style="--pc-soft:var({{ tone_soft }})">
              <span class="gh-badge" style="color:var({{ tone }})">{{ role }}</span>
              {%- if product.featured_image -%}
                {{ product.featured_image | image_url: width: 700 | image_tag: loading: 'lazy', width: 700, height: 560, alt: product.title }}
              {%- else -%}
                <span class="gh-cluster">
                  <span class="gh-g gh-g1">{% render 'gc-gummy', tone: tone, id: gid_a %}</span>
                  <span class="gh-g gh-g2">{% render 'gc-gummy', tone: tone, id: gid_b %}</span>
                  <span class="gh-g gh-g3">{% render 'gc-gummy', tone: tone, id: gid_c %}</span>
                </span>
              {%- endif -%}
            </a>
            <a href="{{ product.url }}" class="gh-name">{{ product.title }}</a>
            <p class="gh-line">{{ benefit }}</p>
            <div class="gh-foot">
              <span class="gh-price" style="color:var({{ tone }})">{{ product.price | money_without_trailing_zeros | replace: 'Rs.', '₹' }}</span>
              {%- if product.available -%}
                <form method="post" action="{{ routes.cart_add_url }}">
                  <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
                  <input type="hidden" name="quantity" value="1">
                  <button type="submit" class="gh-add" style="background:var({{ tone }})">Add</button>
                </form>
              {%- else -%}
                <span class="gh-add gh-add--soldout">Sold out</span>
              {%- endif -%}
            </div>
          </div>
        {%- else -%}
          <p class="gh-empty">Add products in Shopify — they appear here automatically.</p>
        {%- endfor -%}
      </div>
    </div>
  </section>

  <!-- ACT 3 — story -->
  <section class="gh-sec" id="gc-story" style="padding-top:0">
    <div class="gc-wrap">
      <div class="gh-story gc-rise">
        <div class="gh-story-in">
          <p class="gc-kick" style="color:var(--c-bg-warm);opacity:.75">{{ s.story_kicker }}</p>
          <h2>{{ s.story_title | replace: '[', '<em>' | replace: ']', '</em>' }}</h2>
          <p>{{ s.story_body }}</p>
          <div class="gh-vals">
            <div class="gh-val"><b>{{ s.val1_t }}</b><span>{{ s.val1_b }}</span></div>
            <div class="gh-val"><b>{{ s.val2_t }}</b><span>{{ s.val2_b }}</span></div>
            <div class="gh-val"><b>{{ s.val3_t }}</b><span>{{ s.val3_b }}</span></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ACT 4 — FAQ -->
  <section class="gh-sec" id="gc-faq" style="padding-top:0">
    <div class="gc-wrap">
      <div class="gh-head gc-rise">
        <p class="gc-kick">Good to know</p>
        <h2>Questions, answered.</h2>
      </div>
      <div class="gh-faq gc-rise">
        <details class="gh-q"><summary>How many do I take a day?</summary><p>Two gummies a day, any time that suits you. Your daily chums are waiting — no water, no pills, no fuss.</p></details>
        <details class="gh-q"><summary>Is there added sugar?</summary><p>No added sugar. Just the good stuff, in a gummy you’ll actually look forward to.</p></details>
        <details class="gh-q"><summary>Are they vegetarian?</summary><p>Yes — 100% vegetarian, pectin-based, and made in India in an FSSAI-compliant facility.</p></details>
        <details class="gh-q"><summary>How long until I feel a difference?</summary><p>Wellness is a habit, not a switch. Most Chums take theirs daily for 4–6 weeks to feel the difference. Consistency is the whole point.</p></details>
        <details class="gh-q"><summary>How fast is delivery?</summary><p>We ship pan-India. Orders are usually delivered in 3–7 working days, and you’ll get tracking as soon as it leaves us.</p></details>
      </div>
    </div>
  </section>

  <!-- ACT 5 — closing -->
  <section class="gh-end gc-rise">
    <div class="gc-wrap">
      <h2>{{ s.end_title | replace: '[', '<em>' | replace: ']', '</em>' }}</h2>
      <p>{{ s.end_sub }}</p>
      <a class="gc-btn" href="{{ routes.all_products_collection_url }}">{{ s.end_cta }}</a>
    </div>
  </section>
</div>

<script>
(function(){
  /* gummy characters drift toward the cursor — subtle depth */
  var pin=document.getElementById('gh-pin');
  if(pin && matchMedia('(hover:hover) and (pointer:fine)').matches
     && !matchMedia('(prefers-reduced-motion:reduce)').matches){
    var chars=[].slice.call(pin.querySelectorAll('.gh-char'));
    var tx=0,ty=0,cx=0,cy=0,raf=null;
    addEventListener('pointermove',function(e){
      tx=(e.clientX/innerWidth-.5); ty=(e.clientY/innerHeight-.5);
      if(!raf)raf=requestAnimationFrame(run);
    },{passive:true});
    function run(){
      cx+=(tx-cx)*.06; cy+=(ty-cy)*.06;
      chars.forEach(function(el){
        var d=parseFloat(el.dataset.depth)||14;
        el.style.setProperty('--px',(cx*d).toFixed(2)+'px');
        el.style.setProperty('--py',(cy*d).toFixed(2)+'px');
      });
      raf=(Math.abs(tx-cx)>.001||Math.abs(ty-cy)>.001)?requestAnimationFrame(run):null;
    }
  }

  var stage=document.getElementById('gh-stage'); if(!stage)return;
  var tk=false;
  function upd(){var r=stage.getBoundingClientRect();var d=r.height-innerHeight;
    var z=d>0?Math.min(Math.max(-r.top/d,0),1):0;stage.style.setProperty('--z',z.toFixed(4));}
  addEventListener('scroll',function(){if(!tk){requestAnimationFrame(function(){upd();tk=false;});tk=true;}},{passive:true});
  addEventListener('resize',upd);upd();
})();
</script>

{% schema %}
{
  "name": "GummyChums Home",
  "settings": [
    { "type": "header", "content": "Hero" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow chip", "default": "✦ Wellness, but make it fun" },
    { "type": "text", "id": "headline", "label": "Headline (use [ ] to highlight)", "default": "Not another [supplement] brand." },
    { "type": "text", "id": "sub", "label": "Subline", "default": "We’re making healthy habits feel good." },
    { "type": "text", "id": "cta", "label": "CTA label", "default": "Shop the flavours →" },
    { "type": "text", "id": "reveal_title", "label": "Scroll reveal title", "default": "WHAT’S INSIDE?" },
    { "type": "text", "id": "reveal_sub", "label": "Scroll reveal sub", "default": "Bacopa · Melatonin · Lutein. Zero junk — just real wellness." },
    { "type": "header", "content": "Flavours" },
    { "type": "text", "id": "shop_kicker", "label": "Kicker", "default": "Three flavours ✦ one happy habit" },
    { "type": "text", "id": "shop_title", "label": "Title", "default": "Meet the three." },
    { "type": "text", "id": "shop_sub", "label": "Sub", "default": "Supplements you’ll actually look forward to." },
    { "type": "header", "content": "Story" },
    { "type": "text", "id": "story_kicker", "label": "Kicker", "default": "Why we exist" },
    { "type": "text", "id": "story_title", "label": "Title (use [ ] for pink)", "default": "Wellness became a [checklist]. We made it a treat." },
    { "type": "textarea", "id": "story_body", "label": "Body", "default": "People don’t quit because they don’t care — they quit because it feels like work. So we made the healthiest thing you do all day the tastiest. A chum is the friend who reminds you. That’s the whole plan." },
    { "type": "text", "id": "val1_t", "label": "Value 1 title", "default": "Joy, non-negotiable" },
    { "type": "text", "id": "val1_b", "label": "Value 1 body", "default": "If it doesn’t make you smile, it doesn’t ship." },
    { "type": "text", "id": "val2_t", "label": "Value 2 title", "default": "Trust you can read" },
    { "type": "text", "id": "val2_b", "label": "Value 2 body", "default": "The label says everything. Every time." },
    { "type": "text", "id": "val3_t", "label": "Value 3 title", "default": "Within reach" },
    { "type": "text", "id": "val3_b", "label": "Value 3 body", "default": "Premium should never mean pricey." },
    { "type": "header", "content": "Closing" },
    { "type": "text", "id": "end_title", "label": "Title (use [ ] for pink)", "default": "Your daily chums are [waiting]." },
    { "type": "text", "id": "end_sub", "label": "Sub", "default": "Three flavours. One happy habit. Start today." },
    { "type": "text", "id": "end_cta", "label": "CTA", "default": "Grab all three →" }
  ],
  "presets": [{ "name": "GummyChums Home" }]
}
{% endschema %}
